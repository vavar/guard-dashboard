const createResult = require('./create-result');
const listResults = require('./list-results');
const getFindings = require('./get-findings');

module.exports = {
    createResult,
    listResults,
    getFindings,
};
